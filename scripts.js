function addToCart(productName, productPrice) {
    alert(`${productName} has been added to your cart for Ksh ${productPrice}.`);
    // Here, you can add more functionality like updating the cart total, etc.
}
